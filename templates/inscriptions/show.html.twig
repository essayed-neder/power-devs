{% extends 'back.html.twig' %}

{% block title %}Inscriptions{% endblock %}

{% block body %}
    <h1>Inscriptions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idinscri</th>
                <td>{{ inscription.idinscri }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ inscription.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ inscription.prenom }}</td>
            </tr>
            <tr>
                <th>Tel</th>
                <td>{{ inscription.tel }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ inscription.genre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inscriptions_index') }}">back to list</a>

    <a href="{{ path('app_inscriptions_edit', {'idinscri': inscription.idinscri}) }}">edit</a>

    {{ include('inscriptions/_delete_form.html.twig') }}
{% endblock %}
